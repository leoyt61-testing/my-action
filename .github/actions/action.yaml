name: 'My Action'
description: 'This is a reusable action'

runs:
  using: 'composite'
  steps:
    - name: 'Checkout'
      uses: actions/checkout@v3
    - name: 'Run Script'
      shell: bash # Add this line to specify the shell to use
      run: |
          echo "Hello world"