name: 'My Action'
description: 'This is a reusable action'

runs:
  using: 'composite'
  steps:
    - name: 'Checkout'
      uses: actions/checkout@v3
    - name: 'Run Script'
          shell: bash
      run: |
          echo "${{github.repository}}"